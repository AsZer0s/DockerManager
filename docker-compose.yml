version: '3.8'

services:
  dockermanager:
    image: ${DOCKER_IMAGE:-your-dockerhub-username/dockermanager}:latest
    container_name: dockermanager
    ports:
      - "9090:9090"
    volumes:
      - ./data:/app
    environment:
      - GIN_MODE=release
    restart: unless-stopped
