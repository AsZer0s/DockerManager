# Docker Manager 环境变量配置文件
# 复制此文件为 .env 并根据需要修改配置

# =============================================================================
# 核心配置 (系统会自动生成安全密钥，通常不需要修改)
# =============================================================================

# 数据库路径 (容器内路径，不建议修改)
DATABASE_PATH=/app/data/database.sqlite

# JWT 密钥 (系统自动生成，不需要手动设置)
# JWT_SECRET=auto-generated-will-be-set-by-container

# 加密密钥 (系统自动生成，不需要手动设置)
# ENCRYPTION_KEY=auto-generated-will-be-set-by-container

# 运行环境
NODE_ENV=production

# 服务端口
PORT=3000

# 日志级别 (error, warn, info, debug)
LOG_LEVEL=info

# 监控数据收集间隔 (毫秒)
MONITORING_INTERVAL=5000

# CORS 允许的来源 (多个用逗号分隔)
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# =============================================================================
# Telegram 机器人配置 (可选)
# =============================================================================

# Telegram 机器人 Token (从 @BotFather 获取)
# TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Telegram Web App URL
# TELEGRAM_WEBAPP_URL=http://localhost:3000/telegram-webapp

# Telegram 代理设置 (如果需要)
# TGBOT_PROXY=http://127.0.0.1:10808

# =============================================================================
# 邮件服务配置 (可选，用于通知和密码重置)
# =============================================================================

# SMTP 服务器配置
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# SMTP_FROM=Docker Manager <noreply@dockermanager.com>

# =============================================================================
# 数据库配置 (可选，默认使用 SQLite)
# =============================================================================

# 数据库类型 (sqlite 或 mysql)
# DATABASE_CLIENT=sqlite

# MySQL 数据库配置 (如果使用 MySQL)
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=docker_manager
# MYSQL_PASSWORD=your_mysql_password
# MYSQL_DATABASE=docker_manager
# MYSQL_POOL_SIZE=10

# =============================================================================
# 安全配置 (高级用户)
# =============================================================================

# 速率限制窗口时间 (毫秒)
# RATE_LIMIT_WINDOW_MS=900000

# 速率限制最大请求数
# RATE_LIMIT_MAX=100

# 会话密钥 (系统自动生成)
# SESSION_SECRET=auto-generated

# 文件上传大小限制 (字节)
# MAX_FILE_SIZE=10485760

# 上传文件路径
# UPLOAD_PATH=./uploads

# =============================================================================
# 使用说明
# =============================================================================

# 1. 基本使用：
#    系统开箱即用，无需配置任何环境变量即可启动
#
# 2. 启用 Telegram 功能：
#    取消注释 TELEGRAM_BOT_TOKEN 并设置真实的 Bot Token
#
# 3. 启用邮件功能：
#    取消注释 SMTP_* 相关配置并设置邮件服务器信息
#
# 4. 使用 MySQL：
#    取消注释 DATABASE_CLIENT 和 MYSQL_* 相关配置
#
# 5. 自定义域名：
#    修改 CORS_ORIGIN 和 TELEGRAM_WEBAPP_URL 为您的域名
#
# 注意：修改配置后需要重启容器才能生效
#       docker-compose restart